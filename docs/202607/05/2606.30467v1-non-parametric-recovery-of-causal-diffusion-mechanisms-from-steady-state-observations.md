---
title: Non-parametric recovery of causal diffusion mechanisms from steady-state observations
title_zh: 从稳态观测中非参数恢复因果扩散机制
authors: "Richard Schwank, Mathias Drton"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30467v1"
tags: ["query:tsc"]
score: 9.0
evidence: 因果扩散机制恢复
tldr: 实际应用中如基因表达分析只能获取稳态横截面数据，难以推断因果机制。本文假设系统为达到平衡的扩散过程且因果结构已知，证明可非参数识别漂移函数，并设计核估计器。证明了估计器的一致性，并通过交叉验证优化超参数。为从稳态观测恢复因果扩散机制提供了理论保证和实用方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30467v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1504, \"height\": 537, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30467v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1318, \"height\": 621, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30467v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 791, \"height\": 486, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.30467v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 888, \"height\": 281, \"label\": \"Table\"}]"
motivation: 许多科学领域只能采集稳态横截面数据，缺乏时间序列，导致因果机制难以推断。
method: 基于已知因果图，利用非参数核估计从稳态分布恢复扩散过程的漂移函数。
result: 证明了在弱非爆炸条件下漂移函数的非参数可识别性及估计器的一致性。
conclusion: 方法为基因表达等只提供稳态数据的场景提供了因果推断工具，并与生成扩散模型相关。
---

## 摘要
我们考虑根据因果机制在连续时间上演化的稀疏多变量随机系统，并提出仅从横截面数据中恢复系统的时间无穷小转移机制的方法。这种观测范式受到诸如基因表达分析等应用的启发，在这些应用中，破坏性实验技术可能只允许在细胞生命周期内记录一次数据。具体地，我们假设系统遵循一个时间齐次的扩散过程，该过程在观测时间时已达到平衡分布。此外，我们假设因果机制完全由扩散漂移描述，是无环的，并且其因果结构图已知。在此设定下，我们证明完整的因果机制（即漂移函数）可以在弱非爆炸准则下被非参数识别。我们针对这个具有挑战性的逆问题推导了一个非参数核估计器，并证明了其一致性。此外，我们提出了一种用于超参数调优的交叉验证方案，通过模拟展示了估计器的行为，并讨论了与不可逆生成扩散模型和低频采样数据的联系。

## Abstract
We consider sparse multivariate stochastic systems that evolve in continuous time according to a causal mechanism and present methodology to recover the system's time-infinitesimal transition mechanism from mere cross-sectional data. This observational paradigm is motivated by applications such as gene expression analysis, where destructive experimental techniques may only allow recording data once over a cell's lifetime. Precisely, we assume the system follows a time-homogeneous diffusion process that has reached an equilibrium distribution at observation time. Further, we assume the causal mechanism is fully described by the diffusion drift, is acyclic, and its causal structure graph is known. In this setting, we prove that the full causal mechanism, i.e., the drift function, can be non-parametrically identified under a weak non-explosion criterion. We derive a non-parametric kernel estimator for this challenging inverse problem and prove its consistency. Moreover, we propose a cross-validation scheme for hyperparameter tuning, illustrate the behavior of our estimator in simulations, and we discuss connections with irreversible generative diffusion models and low-frequency sampled data.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：在许多科学领域（如单细胞基因测序），只能获得每个个体在单个时间点的横截面数据（稳态分布），无法追踪时间演化。然而，潜在的系统通常由连续时间的因果扩散过程驱动。
- **核心问题**：如何仅从稳态观测数据（i.i.d. 样本）中非参数恢复出因果扩散机制（即漂移函数），并保证可识别性和一致性。
- **整体含义**：本文为因果推断提供了新的理论框架，证明了在已知因果结构图（DAG）且满足弱非爆炸条件下，漂移函数可以被唯一确定，并在实践中给出了可行的核估计方法。该工作桥接了稳态观测与生成扩散模型，为解释不可逆动力学和加速扩散采样奠定了基础。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：利用 Fokker-Planck 方程将未知漂移 \(b\) 与已知稳态密度 \(p\) 联系起来；通过假设漂移具有 DAG 结构（即每个分量只依赖其父节点），并结合 \(L^1(p)\) 可积条件保证非爆炸，从而证明漂移的唯一可识别性。
- **关键技术细节**：
  1. **可识别性证明**：基于弱 Fokker-Planck 方程，通过归纳法沿拓扑顺序证明所有漂移分量被唯一确定。
  2. **非参数核估计器**：针对每个分量 \(b_i\)，使用依赖对应父节点集的 RKHS \(H_i\)，构造一个积分公式将 \(b_i\) 的表达转化为可用数据期望近似的量 \(\zeta_i\)。利用核的反导函数 \(K_{i,y}\)（满足 \(\partial_i K_{i,y}=k_i(\cdot,y)\)）将 \(\zeta_i\) 表示为仅涉及更早节点的项。
  3. **估计流程**（Definition 4.3）：  
     - 对每个节点 \(i\) 按拓扑顺序，计算 \(\hat{\zeta}_i(y)=\frac{1}{n}\sum_{j=1}^n \hat{z}_i(x_j, y)\)，其中 \(\hat{z}_i\) 使用已估计的父节点漂移。  
     - 通过 Tikhonov 正则化或其他正则化（Spectral cut‑off、Landweber）反演积分算子，得到估计 \(\hat{b}_i\)。  
  4. **交叉验证**：基于风险分解（式 (17)），利用测试集估计期望和内核内积，实现超参数（如正则化强度 \(\lambda\)）的自适应选择。
- **数学支撑**：给出浓度不等式（Lemma 4.4–4.6）控制 \(\hat{\zeta}\) 和 \(\hat{L}\) 的误差，进而导出主定理（Theorem 4.8）和一致性推论（Corollary 4.9），表明在适当选择的 \(\lambda_i\) 下，估计在 \(L^2(p)\) 意义下收敛。

### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **场景 1：二维随机 Logistic 种群增长模型**（Section 5.1）  
  - 外生过程 \(x_1\) 影响种群容量；扩散系数部分已知。  
  - 使用 \(n=1000\) 个稳态样本；结构图已知（\(\looparrowright 1\rightarrow 2\looparrowright\)）。  
  - 仅评估了本文提出的 Tikhonov 正则化估计器及其与真实漂移的对比（图 1 左）。

- **场景 2：七维合成 sigmoid 漂移**（Section 5.2）  
  - 漂移定义为父节点加权 tanh 线性组合；扩散为单位矩阵。  
  - 样本量 \(n=10^3\) 到 \(2\times 10^4\)。  
  - **对比方法**：不同正则化方法（Tikhonov、Spectral cut‑off、加速 Landweber）以及不同核（Gaussian、Sigmoid）。  
  - 报告了相对均方误差（中位数）随样本量的变化（图 2）。  
  - 附录 C.1 中还与 KDS（Lorch et al., 2024）方法进行了对比（仅在线性高斯设定下）。

- **场景 3：依赖样本（低频采样）**（Section 5.3）  
  - 从单一扩散路径中不同时间间隔 \(\Delta t\) 采样 1000 个点，考察估计器对自相关的鲁棒性（图 1 右）。  
  - 仅评估本文方法。

- **额外分析**：不可逆扩散加速收敛实验（Section 5.4, Fig. 3），以高斯分布为例展示 DAG 结构漂移比纯 Langevin 动力学更快达到稳态。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

文中**未明确提及**任何 GPU 型号、数量或具体训练时长。所有实验均未报告计算资源细节，仅提到在一些模拟中使用了 Python 包和 MCMC 采样。由于论文侧重于理论和方法验证，计算需求相对较低（最大维度 \(d=7\)，样本量 \(n \leq 2\times 10^4\)），可以推测实验在普通 CPU 或单 GPU 上即可完成。

### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平

- **实验数量**：
  - 二维 Logistic 模型：单组设置（\(n=1000\)），重复模拟？未明确重复次数；仅展示一次结果（图 1 左）。
  - 七维合成数据：对每个样本量（5 个不同 \(n\)）和每个方法（3 种正则化 × 2 种核 = 6 种组合），重复 \(N=100\) 次，报告中位数。
  - 依赖样本实验：10 个不同 \(\Delta t\)，每个重复 \(N=100\) 次。
  - 加速收敛实验：单一高斯设定，不同 \(\omega\) 值，未重复次数？仅展示单次轨迹。
- **充分性**：
  - **正面**：七维实验覆盖了多种正则化方法和核函数，重复次数多，结果稳健；交叉验证自动选择超参数，减少了人为干预。
  - **不足**：未与多种最新基线方法全面比较（仅在线性高斯下与 KDS 做了有限对比）。没有在高维（\(d>10\)）或非线性更强的情况下评估。消融实验（如不同核、不同正则化的单独贡献）不系统。依赖样本实验只评估了本文方法，无对比。
- **公平性**：作者承认 KDS 需要早停（oracle stopping），而本文方法是自适应的（交叉验证），且学习任务更全面（包括自反馈），但比较设置稍有不同。其他方面基本公平。

### 6. 论文的主要结论与发现

1. **非参数可识别性**：在已知 DAG 结构和弱非爆炸条件下，因果扩散漂移函数可以被稳态分布唯一确定（Theorem 3.7）。
2. **一致估计**：提出的核估计器在 \(L^2(p)\) 下一致（Corollary 4.9），收敛速度受拓扑深度影响，但可通过适当选择 \(\lambda_i\) 实现。
3. **实践经验**：交叉验证可有效选择正则化强度；Sigmoid 核在匹配真实漂移形式时性能更优；Spectral 和 Landweber 正则化通常优于 Tikhonov。
4. **依赖样本的鲁棒性**：当下采样间隔使自相关中等时，估计器对依赖样本仍有一定鲁棒性（图 1 右）。
5. **生成模型加速**：任何平滑密度都可以构造一个 DAG 结构漂移（Theorem 5.1），该漂移可替代 Langevin 动力学，加速扩散收敛（图 3）。

### 7. 优点：方法或实验设计上有哪些亮点

- **理论贡献**：首次在非参数框架下证明 DAG 结构扩散漂移的可识别性，且条件极弱。
- **方法设计**：巧妙利用积分算子逆，将未知漂移转化为可通过样本期望计算的量，避免了直接求解 Fokker-Planck PDE 的维数灾难。
- **正则化多样性**：支持多种正则化方法（Tikhonov、Spectral cut‑off、Landweber），并给出统一的理论分析。
- **交叉验证**：提出基于风险分解的交叉验证方案，适应数据驱动选择超参数。
- **实验验证**：涵盖二维至七维，不同核和正则化，重复多次，结果可靠。
- **延伸讨论**：与不可逆生成扩散模型的联系提供额外启示（加速采样）。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **假设强**：需要预先知道精确的 DAG 结构，这在许多真实系统中难以获得；循环图或未知结构的情况未处理。
- **可扩展性**：理论收敛速率随拓扑深度指数增长（\(r_i \sim 3^{d_i}\)），可能在高维稀疏图中变得很慢。
- **计算成本**：Tikhonov 和 Spectral 方法因求逆而具有 \(O(n^3)\) 复杂度（虽然用了 Nyström 近似，但仍有限制）；Landweber 为 \(O(n^2)\)，但步数选取离散。
- **实验局限**：未在高维（如 \(d>10\)）或真实生物数据上测试；对比方法不足；依赖样本实验中未理论分析一致性的条件。
- **理论假设**：分析中假设漂移有界（\(L^\infty\)）以简化推导，实际应用中很多漂移是无界的，文中承认这一限制并指出实证表现良好，但仍需更一般化的理论。
- **未知扩散率**：扩散函数 \(\Sigma\) 假定已知，这在实践中通常不可得，是重要的开放问题。
- **递归误差累积**：沿拓扑顺序的插值策略可能导致父节点误差指数传播，实验中发现父节点数量比深度更影响误差，但理论上仍需改进。

（完）
