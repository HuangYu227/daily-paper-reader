---
title: Primary ICD Category Prediction using LLM-based Probing
title_zh: 使用基于LLM的探测进行主要ICD类别预测
authors: "Chengyuan Liu, Xinyue Zhang, Yao Li, Guanting Chen"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28798v1"
tags: ["query:fe"]
score: 8.0
evidence: 使用冻结的医学大语言模型作为固定编码器进行ICD分类预测
tldr: "针对自动ICD编码难以融合临床叙述和结构化电子病历信号的问题，提出利用冻结医学大语言模型（MedFound-Llama3-8B）作为共享嵌入空间，通过线性探针实现多模态主要诊断类别预测。在MIMIC-IV数据集上，结合结构化序列化文本和出院记录的探针达到87.69%严格准确率和91.45%医学准确率，优于单模态和基线方法；深层表示线性可分性更强，2M参数适配器仅用5%标注即可跨数据集迁移。该方法为多模态临床表示的高效复用和研究提供了实用途径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28798v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1849, \"height\": 1022, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28798v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 895, \"height\": 631, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28798v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 942, \"height\": 845, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28798v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 589, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28798v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 714, \"height\": 139, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28798v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 928, \"height\": 175, \"label\": \"Table\"}]"
motivation: 现有自动ICD编码系统难以有效融合临床文本和结构化数据中的诊断信号，亟需一种统一的嵌入空间来联合建模多模态信息。
method: 使用冻结的医学LLM（MedFound-Llama3-8B）提取多层隐藏状态，训练线性探针处理序列化的结构化变量和出院记录，进行多模态主要诊断类别预测。
result: "组合探针在MIMIC-IV上取得87.69%严格准确率，结构化探针比XGBoost基线高6.19个百分点；深层表示线性可分离性更强，2M参数适配器实现跨数据集迁移。"
conclusion: 多模态探测冻结医学LLM表示能有效统一结构化与非结构化信息，为跨数据集临床表示迁移提供轻量级方案。
---

## 摘要
目的：ICD编码对于报销、研究和人群健康监测至关重要，然而自动编码系统常常难以整合来自临床叙述和结构化电子健康记录变量的诊断信号。我们评估了冻结的医学大语言模型表示是否可以作为多模态主要诊断类别预测的共享嵌入空间。材料与方法：我们从MIMIC-IV数据库中构建了一个包含13,645次住院的队列，这些住院来自最常见的10个主要ICD-10代码，合并为七个类别。结构化变量被序列化为临床叙述，并与经过泄漏修剪的出院记录相结合。使用冻结的MedFound-Llama3-8B微调骨干网络，我们从五个变换器层提取隐藏状态，并为结构化仅、非结构化仅以及组合输入训练线性探测，与XGBoost和信息匹配的PLM-ICD基线进行比较，并使用紧凑瓶颈适配器评估MIMIC-III的适应性能。结果：组合探测在MIMIC-IV上表现最佳（严格准确率87.69%；医学准确率91.45%），超过了单模态探测和基线。结构化仅探测的医学准确率比其标准基线高出6.19个百分点。诊断信息在更深层中变得越来越线性可分，一个2M参数的适配器仅使用5%的目标标签就恢复了跨数据集迁移到MIMIC-III的能力。讨论：LLM嵌入可以统一结构化和叙述性EHR信息以进行多模态诊断预测，通过一个小规模的表示级模块支持跨模态和数据集有效重用临床表示。结论：对冻结的医学LLM表示进行多模态探测为研究EHR模态和跨数据集适应临床表示提供了一种实用方法。

## Abstract
Objective: ICD codes are central to reimbursement, research, and population health surveillance, yet automated coding systems often struggle to integrate diagnostic signals from both clinical narratives and structured electronic health record (EHR) variables. We evaluated whether frozen medical large language model (LLM) representations can serve as a shared embedding space for multimodal primary diagnosis category prediction.   Materials and Methods: We constructed a MIMIC-IV cohort of 13,645 admissions from the 10 most frequent primary ICD-10 codes, consolidated into seven categories. Structured variables were serialized into clinical narratives and combined with leakage-pruned discharge notes. Using a frozen MedFound-Llama3-8B-finetuned backbone, we extracted hidden states from five transformer layers and trained linear probes for structured-only, unstructured-only, and combined inputs, comparing against XGBoost and information-matched PLM-ICD baselines and evaluating MIMIC-III adaptation with a compact bottleneck adapter.   Results: The combined probe performed best on MIMIC-IV (87.69% strict; 91.45% medical accuracy), exceeding both single-modality probes and baselines. The structured-only probe outperformed its standard baseline by 6.19 points in medical accuracy. Diagnostic information became increasingly linearly separable in deeper layers, and a 2M-parameter adapter restored cross-dataset transfer to MIMIC-III using only 5% of target labels.   Discussion: LLM embeddings can unify structured and narrative EHR information for multimodal diagnosis prediction, supporting efficient reuse of clinical representations across modalities and datasets through a small representation-level module.   Conclusion: Multimodal probing of frozen medical LLM representations provides a practical approach for studying EHR modalities and adapting clinical representations across datasets.

---

## 论文详细总结（自动生成）

### 论文详细总结

#### 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：自动ICD编码系统难以有效整合来自临床叙述（出院记录）和结构化电子健康记录（实验室值、用药等信息）的诊断信号，两者往往是互补的，但现有方法大多依赖模态特定架构或任务特定融合模块，成本高且复用性差。
- **研究动机**：探索是否可以利用**冻结的医学大语言模型（LLM）** 作为共享嵌入空间，将结构化和非结构化EHR数据统一表示，从而在不微调骨干网络的情况下进行多模态主要诊断类别预测。
- **整体含义**：若可行，则提供一种高效、可复用的方式研究EHR模态间的诊断信号，并通过轻量级适配器实现跨数据集迁移，无需为每个新任务重新训练大型模型。

#### 2. 方法论
- **核心思想**：将结构化变量（如年龄、性别、实验室结果、用药）通过模板序列化为临床叙述文本，与经过泄漏修剪（去除直接陈述诊断的章节）的出院记录文本结合，组成统一文本输入。使用**冻结的 MedFound-Llama3-8B** 骨干网络提取隐藏状态，对每个配置（结构化仅、非结构化仅、组合）和每个选定层（1、8、16、24、32）分别训练线性探针。
- **关键技术细节**：
  - **输入构造**：结构化→模板 `Tstr`；非结构化→章节提取函数 `Ttxt`；组合→两者拼接。
  - **泄漏修剪**：去掉“出院诊断”“最终印象”等章节，避免标签直接泄露。
  - **嵌入提取**：对每个样本，在指定层对非填充令牌进行平均池化，得到 4096 维表示 `h`。
  - **线性探针**：`z = W h + b`，其中 `W ∈ R^(7×4096)`，偏置 `b ∈ R^7`，最小化交叉熵损失。仅有探针参数被优化，骨干网络完全冻结。
  - **跨数据集迁移**：使用 2M 参数的瓶颈适配器（降维 4096→256→4096，残差连接加层归一化）调整MIMIC-IV的嵌入，以适应MIMIC-III分布，骨干和MIMIC-IV探针均冻结。
- **公式/算法流程**：无复杂公式，核心为线性映射；适配器公式：`u = ReLU(W_down h)`, `\hat{h} = LayerNorm(h + W_up u)`，`W_up` 零初始化以保证初始为恒等映射。

#### 3. 实验设计
- **使用的数据集/场景**：
  - **MIMIC-IV**（主要开发与评估）：13,645 次住院，前10个最频繁 ICD-10 主诊断代码合并为7个临床类别。70%/15%/15% 训练/验证/测试分割。
  - **MIMIC-III**（跨数据集泛化）：13,137 次住院，前10个 ICD-9 代码合并为7个类别，其中5个可映射到 MIMIC-IV 类别。
- **Benchmark（基线）**：
  - **结构化基线**：XGBoost（使用实验室、生命体征、药物特征的传统梯度提升模型）。
  - **非结构化基线**：PLM-ICD（基于BERT的文本专用ICD编码模型），使用相同分割、泄漏修剪和标签空间。
- **对比方法**：
  - 探针自身：结构化仅、非结构化仅、组合三个配置，五个层比较。
  - 与 XGBoost 和 PLM-ICD 对比。
  - 跨数据集迁移：零样本直接迁移；适配器（仅5% MIMIC-III 标签，即361样本）微调后再评估。

#### 4. 资源与算力
- **文中未明确说明**使用的 GPU 型号、数量、训练时长等硬件细节。仅提到 MedFound-Llama3-8B 作为骨干（8B参数），适配器仅 2M 参数训练。未提及探针训练的计算成本。

#### 5. 实验数量与充分性
- **实验组数**：
  - MIMIC-IV：3种配置 × 5层 = 15组探针实验；加上 XGBoost、PLM-ICD 基线；共约17组主要结果。另有 PLM-ICD 全文本敏感性分析（单独报告）。
  - MIMIC-III：3种配置 × 5层 探针（完整MIMIC-III实验，见补充表）；跨数据集转移：1组零样本、3组配置适配器（5%标签）。
  - 消融分析：如层深度趋势、软匹配规则下的表现、每类召回。
- **是否充分、客观、公平**：
  - **公平**：所有方法使用相同的患者级分割、相同的泄漏修剪、相同的7类别标签空间；PLM-ICD 采用信息匹配预算（仅使用与非结构化探针相同长度的文本？文中说“matched information budget”，但敏感性分析也用全文本）。
  - **充分**：涵盖结构化、非结构化、组合的全面比较；覆盖两个不同时期的数据集；评估指标包含严格准确率、医学准确率、AUROC、AUPRC，并进行统计检验（McNemar、bootstrap、BH校正）。
  - **局限**：仅限前10个最常见代码，不覆盖完整长尾；单中心 ICU 数据。

#### 6. 主要结论与发现
- **性能最佳**：组合探针（Layer 32）在 MIMIC-IV 上达到 **87.69% 严格准确率** 和 **91.45% 医学准确率**，超过单模态探针和所有基线。
- **结构化探针提升**：结构化仅探针医学准确率 87.79%，比 XGBoost 基线（81.60%）高 **6.19 个百分点**，说明序列化后通过冻结LLM能更好捕捉结构化信号。
- **深度趋势**：诊断信息的线性可分性随层数加深而增加，深层（32层）表现最好。
- **跨数据集迁移**：零样本直接迁移效果差（44.5%-79.7%），但使用 2M 参数适配器 + 仅 5% MIMIC-III 标签（361样本）即可恢复组合配置至 92.2% 准确率，说明跨数据集失配可通过轻量级表示对齐解决。
- **与PLM-ICD对比**：组合探针在严格准确率上微弱优于PLM-ICD（87.69% vs. 86.47%），差异未达统计显著；但在宏观AUROC/AUPRC上显著更好。PLM-ICD 使用全文本时可达到 91.55%，但信息匹配下组合探针不劣于。

#### 7. 优点
- **创新性**：将结构化数据通过模板转化为自然语言，无需独立编码器，同一冻结LLM即可同时处理两种模态；探针轻量，属性诊断信号可归因于表示本身而非模型容量。
- **实用性与复用性**：冻结骨干，相同嵌入支持多配置、多层、多模态、多提示格式的重复分析；跨数据集迁移通过小型适配器实现，无需重训大模型。
- **透明性与统计 rigor**：全面报告严格准确率、医学准确率、每类召回；进行统计显著性检验；泄漏修剪严格，确保公平比较。
- **跨数据验证**：同时在MIMIC-IV和MIMIC-III上实验，验证层深度趋势的复用性和适配器的有效性。

#### 8. 不足与局限
- **任务简化**：仅预测前10个最常见主诊断代码，合并为7类，不能代表真实生产环境中的多标签、长尾分布ICD编码任务。
- **数据源局限**：回顾性单中心ICU数据（MIMIC），可能无法推广到门诊、非ICU或不同机构。
- **时间信息丢失**：模板化序列化将纵向实验室/用药轨迹压缩为静态文本，丢失精细时间动态（如趋势、变化率）。
- **医学准确率解释需谨慎**：该软匹配指标对临床相关但非完全相同预测给予分数，可能高估实际精确匹配能力。
- **骨干泛化性**：仅使用MedFound-Llama3-8B一个LLM，未验证其他医学LLM是否一致。
- **无前瞻验证**：缺乏与专业编码员的工作流程集成评估。
- **计算资源未报告**：未说明探针训练或骨干推理的实际GPU需求，不利于复现或成本估计。

（完）
