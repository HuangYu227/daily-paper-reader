---
title: Time-Series Foundation Model Embeddings for Remaining Useful Life Estimation
title_zh: 用于剩余使用寿命估计的时间序列基础模型嵌入
authors: "Amir El-Ghoussani, Michele De Vita, Ronald Naumann, Valiseios Belagiannis"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11990v1"
tags: ["query:fe"]
score: 8.0
evidence: 使用冻结的预训练时间序列基础模型作为编码器
tldr: 剩余使用寿命预测对工业预测维护至关重要，但现有方法依赖特征工程或大量标注数据。本文提出一种轻量学习方法，利用冻结的预训练时间序列基础模型Chronos-2提取上下文窗口特征，再训练一个轻量回归神经网络进行RUL预测。在真实工业传感器数据上的实验表明，该方法在相同预处理和评估协议下一致优于RNN、CNN、Transformer和梯度提升基线，且更长的历史窗口显著提升性能。该工作表明时间序列基础模型表示为工业RUL估计提供了实用且数据高效的替代方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11990v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 929, \"height\": 758, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11990v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 924, \"height\": 420, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11990v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 835, \"height\": 359, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11990v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 587, \"height\": 195, \"label\": \"Table\"}]"
motivation: 现有RUL预测方法依赖特征工程或大量标注数据，缺乏通用且数据高效的解决方案。
method: 冻结预训练的Chronos-2模型作为骨干，提取上下文窗口特征，接轻量回归网络进行RUL预测。
result: 在两种工业传感器数据上，该方法一致优于RNN、CNN、Transformer和梯度提升基线，且更长历史窗口提升性能。
conclusion: 时间序列基础模型为工业RUL估计提供了实用且数据高效的方案，减少对大规模标注数据的依赖。
---

## 摘要
剩余使用寿命（RUL）预测对于工业预测性维护至关重要，然而许多基于学习的方法依赖于大量的特征工程或大规模标注数据集来训练特定任务的序列模型。在这项工作中，我们引入了一种轻量级学习方法，利用冻结的预训练时间序列基础模型（TSFM），并将其与一个小型回归头相结合，用于从多变量传感器流中估计RUL。具体而言，我们使用Chronos-2作为冻结骨干网络来提取上下文窗口特征，并训练一个轻量级回归神经网络进行RUL预测。在来自两种设备类型的真实工业传感器数据上的实验表明，在相同的预处理和评估协议下，Chronos-2特征一致优于循环、卷积、基于Transformer和梯度提升基线。我们进一步分析了上下文长度的影响，发现更长的历史记录能显著提升性能，这表明TSFM表示为工业环境中的RUL估计提供了一种实用且数据高效的替代方案。

## Abstract
Remaining Useful Life (RUL) prediction is essential for industrial predictive maintenance, yet many learning-based approaches rely on extensive feature engineering or large labeled datasets to train task-specific sequence models. In this work, we introduce a lightweight learning approach, in which we leverage a frozen pretrained time-series foundation model (TSFM) and combine it with a small regression head for RUL estimation from multivariate sensor streams. More specifically, we use Chronos-2 as a frozen backbone to extract context window features and train a lightweight regression neural network for RUL prediction. Experiments on real-world industrial sensor data from two device types show that Chronos-2 features consistently improve over recurrent, convolutional, Transformer-based, and gradient-boosting baselines under the same preprocessing and evaluation protocol. We further analyze the impact of context length and find that performance improves significantly with longer histories, indicating that TSFM representation offer a practical and data-efficient alternative for RUL estimation in industrial settings.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：工业预测性维护中，剩余使用寿命（RUL）估计是关键任务，但现有基于学习的方法严重依赖大量标注数据和手工特征工程，泛化性差且资源消耗大。
- **研究动机**：探索是否可以利用冻结的预训练时间序列基础模型（TSFM）作为通用特征提取器，以轻量级方式从多变量传感器流中高效预测RUL，从而减少对标注数据和任务特定训练的依赖。
- **整体含义**：将TSFM从传统的预测任务扩展到RUL回归，验证了基础模型在工业资产退化建模中的潜力，提供了一种实用且数据高效的替代方案。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式与流程
- **核心思想**：冻结预训练的Chronos-2模型作为骨干，从传感器历史窗口中提取上下文嵌入，再训练一个小型回归头进行RUL预测。整个流程仅更新回归头参数，保持骨干冻结，实现轻量级学习。
- **关键技术细节**：
  - **数据预处理**：由于原始数据时间戳不规则，采用线性插值到固定时间网格（步长∆t = 1小时）；舍弃间隙过大的区间；进行离群值截断（1%/99%分位数）和Z-score归一化（仅基于训练集计算统计量）。
  - **RUL标签构造**：从维修日志中获取故障时间戳，定义RUL = 下一故障时间 - 当前时间，上限为1000天；排除无后续维修事件的时间点。
  - **模型架构**：使用Chronos-2（解码器Transformer）的`chronos2`检查点，输入窗口长度为L的传感器序列`X_{t-L:t}`，提取隐藏表示`H = Φ_e(X_{t-L:t}) ∈ R^{L×h}`，其中h为隐藏维度；回归头g_φ为2层MLP（隐藏宽度m，ReLU激活，dropout率p），输出非负RUL预测`ŷ_t = g_φ(H)`。
  - **训练策略**：仅优化MLP参数，损失函数为MSE，优化器Adam，学习率1e-3，训练50个epoch，batch size 64。

## 3. 实验设计：数据集、基准与方法对比
- **数据集**：来自Nokia的两种工业设备传感器数据：
  - Device A：87个传感器通道，297,345个标注样本。
  - Device B：51个传感器通道，119,364个标注样本。
  - 均以1小时为时间步长，上下文窗口L=5（主要实验）。
- **基准与对比方法**：
  - **非序列方法**：线性回归、随机森林（基于单点特征）。
  - **序列方法**：GRU、LSTM、TCN、Transformer编码器（均以窗口输入直接预测）；梯度提升（基于窗口内统计特征：均值、标准差、最小值、最大值、分位数、最后值、一阶差分、线性趋势）。
- **评估指标**：MAE（平均绝对误差）和MSE（均方误差）。
- **划分方式**：严格按时间顺序划分训练/测试（85:15），并丢弃跨越边界的时间窗口，防止时序泄漏。超参数在验证集（约10%训练数据）上调优。

## 4. 资源与算力
- **训练资源**：单个NVIDIA A6000 GPU，每设备训练约2小时（50个epoch）。
- **模型规模**：Device A约300K参数，Device B约250K参数（仅头参数量）。
- **未明确说明**：论文未提及基线模型的具体算力消耗，也未对比训练总时间。

## 5. 实验数量与充分性
- **实验组数**：
  - **表1**：在L=5下对比所有方法在Device A和B上的性能（共2个场景×8个方法）。
  - **图2**：改变上下文长度L（0~100步），对比Ours、TCN、Transformer、GRU、LSTM、GradBoost在Device A上的MAE变化（1个场景×6个方法×6个L值）。
  - **表2**：回归头消融实验（线性、2层MLP、4层MLP），仅Device A（3种头）。
  - 整体约12组核心对比结果。
- **充分性与公平性**：
  - **充分**：覆盖了传统非序列、序列深度模型、梯度提升等主流基线；考虑了上下文长度的影响；进行了头部容量消融。
  - **公平**：所有方法使用相同的预处理流程、时间划分和指标；基线的超参数基于验证集调整；Chronos-2冻结加MLP头的方式避免了全模型微调的算力优势。
  - **局限**：仅在两种工业设备上评估，未在公开标准基准（如C-MAPSS）上验证；未测试少量标注或缺失传感器等更困难场景。

## 6. 论文的主要结论与发现
- **核心结论**：冻结的Chronos-2嵌入配合轻量MLP头在MAE/MSE上**显著优于**所有基线（如相比TCN，MAE从88降至44，降低2倍）。
- **上下文长度影响**：随着L从5增加到80，性能大幅提升（MAE从约44降至约20左右），超过80后饱和；而基线受L影响很小，说明TSFM能有效利用长历史依赖。
- **头部消融**：线性头在冻结嵌入上已优于TCN（MAE 60 vs. 88），2层MLP进一步改善（MAE 44），但4层MLP无显著提升（MAE 45），证明性能增益主要来自TSFM骨干而非复杂回归头。

## 7. 优点：方法或实验设计上的亮点
- **方法创新**：首次将冻结的TSFM（Chronos-2）应用于RUL回归，保持模型轻量（仅训练MLP），避免了全模型微调的高成本。
- **数据效率**：无需大量标注数据或任务特定预训练，直接利用预训练通用特征，适合工业中标注有限场景。
- **实验严谨**：严格时序分割避免数据泄漏；对比方法全面（包括传统ML和多种深度序列模型）；对上下文长度进行系统分析，揭示TSFM的优势。
- **实用价值**：方法简单易部署，训练时间仅2小时，参数少，适合边缘计算或快速工业适配。

## 8. 不足与局限
- **实验覆盖有限**：
  - 仅使用两种工业传感器设备，未在公开基准（如C-MAPSS）上验证，泛化性存疑。
  - 未评估模型在不同操作条件、缺失传感器或分布漂移下的鲁棒性。
  - 未考虑多变量通道间的交互（Chronos-2为单变量设计，本文通过展平或拼接处理，但未描述具体方式）。
- **缺乏不确定性量化**：当前仅输出点预测，未利用Chronos-2的概率能力（作者列为未来工作），难以支持风险敏感的维护决策。
- **标签依赖**：RUL构造依赖准确维修日志，实际场景中可能缺失或标注噪声大；上限1000天的假设可能不适用所有设备。
- **计算资源细节缺失**：未对比基线方法的训练时间或推理成本，削弱了“轻量”优势的量化说服力。
- **潜在偏差**：Chronos-2是2025年发布的模型，可能已在其训练数据中接触过类似工业传感器模式，存在“数据污染”风险，但论文未讨论。

（完）
