---
title: Diffusion Models for Adaptive Sequential Data Generation
title_zh: 自适应序列数据生成的扩散模型
authors: "Haoyang Cao, Minshuo Chen, Yinbin Han, Renyuan Xu"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06007v1"
tags: ["query:tsc"]
score: 9.0
evidence: 扩散模型用于自适应时间序列生成
tldr: 生成逼真的序列数据对运筹、金融、医疗等领域至关重要，但现有扩散模型在序列生成中未能捕捉时间依赖和适应信息结构。本文提出顺序前向-反向扩散框架，通过逐步加噪去噪并条件于生成历史确保适应性和无未来信息泄露。引入新分数匹配目标实现高效并行训练，在理论层面给出分数近似、估计及分布估计的统计保证，并以ReLU网络为实例。实验在ARMA、高斯过程等合成数据上验证有效性，成功用于构建均值方差最优投资组合，展示了实际应用价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06007v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1682, \"height\": 1033, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06007v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1699, \"height\": 580, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.06007v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1383, \"height\": 322, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.06007v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1633, \"height\": 639, \"label\": \"Table\"}]"
motivation: 现有扩散模型直接扩展至序列数据时无法避免未来信息泄露，缺乏对时间依赖的适应建模。
method: 提出顺序前向-反向扩散框架，沿序列逐步注入和移除噪声，条件于已生成历史，并设计新分数匹配目标实现并行训练。
result: 在合成数据（ARMA、高斯过程）上有效，并能用于构建均值方差最优投资组合。
conclusion: 所提框架实现了无前瞻的适应性时间序列生成，具有理论保证和实证效果。
---

## 摘要
在运筹学、金融、医疗、能源系统和科学计算等现实应用中，生成逼真的合成序列数据至关重要，这些应用中时间索引观测值用于预测、模拟、风险评估和数据驱动的决策。虽然扩散模型在生成静态数据方面取得了显著成功，但将其直接扩展到序列设置往往无法捕捉时间依赖性和信息结构。因此，设计能够以自适应方式（即不预先预测未来信息）模拟序列数据的扩散模型仍然是一个开放挑战。在这项工作中，我们提出了一个用于自适应时间序列生成的序列前向-后向扩散框架。我们的方法沿着序列逐步注入和去除噪声，并基于先前生成的历史进行条件处理以确保自适应性。引入了一种新颖的分数匹配目标以实现高效的并行训练。我们在一个通用框架下推导了严格的统计保证，然后以ReLU网络作为具体实例，建立了分数近似、分数估计和分布估计的结果。在实验上，我们在包括ARMA模型和高斯过程的合成数据上验证了我们的方法，并展示了其在构建均值方差最优投资组合中的有效性。

## Abstract
Generating realistic synthetic sequential data is critical in real-world applications across operations research, finance, healthcare, energy systems, and scientific computing, where time-indexed observations are used for prediction, simulation, risk assessment, and data-driven decision-making. While diffusion models have achieved remarkable success in generating static data, their direct extensions to sequential settings often fail to capture temporal dependence and information structure. Designing diffusion models that can simulate sequential data in an adapted manner, and hence without anticipation of future information, therefore remains an open challenge.   In this work, we propose a sequential forward-backward diffusion framework for adapted time series generation. Our approach progressively injects and removes noise along the sequence, conditioning on the previously generated history to ensure adaptiveness. A novel score-matching objective is introduced for efficient parallel training. We derive rigorous statistical guarantees under a generic framework, then establish score approximation, score estimation, and distribution estimation results with ReLU networks serving as a concrete instance. Empirically, we validate our method on synthetic data, including ARMA models and Gaussian processes, and demonstrate its effectiveness in constructing mean-variance optimal portfolios.

---

## 论文详细总结（自动生成）

# 论文《Diffusion Models for Adaptive Sequential Data Generation》详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题**：生成真实合成序列数据（如金融时间序列、医疗、能源系统等）时，必须保留数据的时间依赖性和信息流方向——即每个时刻的生成值只能依赖于过去和当前信息，不能使用未来信息（称为“适应性”或“非预测性”）。传统的扩散模型直接对整个序列加噪/去噪，会破坏这种因果关系，导致生成数据中引入未来信息泄露。
- **背景**：Transformer自回归模型在离散序列（如文本）中成功，但在连续型时间序列中脆弱且需要量化；现有扩散模型主要针对静态数据或全轨迹联合分布，缺乏对信息流结构的约束。这限制了扩散模型在需要遵循信息流的关键任务（如压力测试、组合优化、仿真决策）中的应用。
- **核心目标**：设计一种既能匹配序列数据联合分布，又能保证每个生成值仅依赖于过去历史（适应性）的扩散框架，并提供严格的统计学习理论保证。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：提出**自适应序列扩散模型（AD-Seq）**，沿时间轴逐坐标（逐时刻）进行前向加噪和逆向去噪，每个坐标的逆向过程条件于已生成的过去历史，从而自然地保证适应性。
- **关键技术细节**：
  - **条件前向过程**：对每个时间步 \( h \)，定义一维OU过程 \( dX_h^t = -\frac{1}{2}X_h^t dt + dW_h^t \)，其初始分布为条件分布 \( p(X_h^0 | X_{[1:h-1]}^0) \)（即给定已生成历史）。
  - **逆向采样过程**：对每个 \( h \)，逆向SDE为 \( d\bar{X}_h^t = \left[ \frac{1}{2}\bar{X}_h^t + \partial_x \log \phi_h^{\bar{X}_{[1:h-1]}^T}(T-t, \bar{X}_h^t) \right] dt + d\bar{W}_h^t \)，其中条件分数 \(\partial_x \log \phi_h^z\) 取决于历史 \( z \)。
  - **并行训练**：设计去噪分数匹配目标（式14），允许对所有时间步的分数函数进行并行训练（而不是顺序采样），提高了效率。
  - **历史截断机制**：为应对长序列，基于时间依赖性衰减假设，仅用最近 \( d_{\text{trunc}} \) 个历史值作为分数函数的输入，平衡统计效率和信息损失。
  - **算法流程（Algorithm 1）**：
    1. 对 \( h=1 \)：使用标准正态初始化，运行反向SDE至 \( T-t_0 \)，得到 \( \tilde{X}_1^{T-t_0}=x^1 \)。
    2. 对于 \( h=2\) 到 \( H \)：给定已生成历史 \( x^{[1:h)} \)，使用条件分数网络 \( \hat{s}_h \)，初始化 \( \tilde{X}_h^0 \sim \mathcal{N}(0,1) \)，运行反向SDE，得到 \( \tilde{X}_h^{T-t_0}=x^h \)。
    3. 输出完整轨迹 \( x^{[1:H]} \)。
- **理论框架**：建立分数近似（利用局部多项式逼近和ReLU网络）、分数估计（基于覆盖数的统计误差界）和分布估计（总变差距离）的有限样本保证。在历史截断假设下，得到率 \( \tilde{O}(n^{-\beta/(4(d_{\text{trunc}}+\beta)}) \)，其中 \( \beta \) 为分布光滑参数，\( d_{\text{trunc}} \) 为有效历史长度。

## 3. 实验设计：数据集 / 场景、Benchmark、对比方法
- **合成数据**：
  - **ARMA(2,2)模型**：系数 \( (\phi_1,\phi_2)=(0.9,-0.01) \)，\( (\theta_1,\theta_2)=(0.1,0.1) \)。序列长度 \( H=500 \)，样本量 \( n \in \{2^8,2^9,2^{10},2^{11}\} \)。评估指标：ACF前40阶的 \( \ell_1 \) 相对误差（Diff_RE_1 vs Emp_RE_1）。
  - **高斯过程（GP）**：指数核 \( \gamma(i,j)=\exp(-|i-j|^\nu/\ell) \)，\( \ell=4, \nu=0.8 \)，序列长度 \( H=32 \)。训练样本 \( n=2048 \)，评估协方差矩阵的重建质量。
- **真实数据（下游决策任务）**：
  - **均值方差组合优化**：使用标普500指数1990-2009年日收益数据（分为40条半年期轨迹），测试在2010-2019年数据上的表现。
  - **对比方法**：
    - **Plug-in策略**：估计GBM系数后代入解析最优策略。
    - **RL策略**：基于连续时间Q-learning[6]的无模型方法。
    - 数据来源：Split（仅40条真实路径）、Split+Synthetic（加入由[7]方法生成的40条合成路径）、Split+Ours（加入由AD-Seq生成的40条合成路径）、Bootstrap（仅用于RL，40条bootstrap路径）。
  - 评估指标：均值、方差、夏普比率（Sharpe Ratio），两个目标水平 \( z=1.10 \) 和 \( z=1.20 \)。

## 4. 资源与算力
- **未明确说明**：论文未提及所使用的GPU型号、数量、训练时长或总计算量。在实验部分仅提到“使用具有因果掩码的Transformer架构”，未提供具体硬件配置。

## 5. 实验数量与充分性
- **数量**：
  - ARMA实验：4个样本量，每个重复5次，报告均值和标准差。
  - GP实验：仅1组（n=2048），定性比较协方差热力图。
  - 组合优化实验：2个目标水平（z=1.10,1.20），每种策略下重复（标准差不明确但报告了）。
- **全面性评估**：
  - **优点**：覆盖了合成数据和真实下游任务，验证了时间依赖恢复和决策改进。
  - **不足**：
    - 缺乏消融研究：未分析历史截断长度 \( d_{\text{trunc}} \) 的影响、未比较不同网络架构（如MLP vs Transformer）、未分析训练样本大小敏感性。
    - 仅一种真实金融场景（S&P 500），未扩展到其他领域（如医疗、能源）。
    - 未与当前其他序列扩散模型（如Diffusion-TS[45]、TSGM[44]）直接对比。
    - 实验复现性：代码已公开（GitHub），但缺少超参数、训练细节（学习率、batch size、迭代次数等）。

## 6. 论文的主要结论与发现
- AD-Seq框架能够生成**适应信息流**的合成时间序列，且通过理论证明其分布估计误差随样本量增加而衰减。
- 在ARMA数据上，AD-Seq生成的样本估计的ACF比直接使用训练样本估计更准确（尤其在小样本下，如n=256时Diff_RE_1=0.1171 vs Emp_RE_1=0.1431）。
- 在GP数据上，生成的样本协方差矩阵能准确恢复指数衰减模式。
- 在组合优化中，使用AD-Seq生成数据训练的RL策略在所有方法中**取得了最高的夏普比率**（如z=1.10时0.9968，z=1.20时0.9965），优于Split+Synthetic和Bootstrap。
- 表明尊重时间依赖和信息流能提升下游决策质量。

## 7. 优点：方法或实验设计上的亮点
- **方法亮点**：
  - 首次为扩散模型引入**适应性保证**，通过逐坐标条件生成从根本上避免未来信息泄露，具有理论严谨性。
  - **并行训练**：通过去噪分数匹配目标将分数学习解耦到各时间步，可并行训练，可扩展性强。
  - **历史截断**：基于时间依赖性衰减假设，使长序列生成在统计上可处理，并给出指数/多项式衰减下的最优截断长度选择。
  - **通用理论框架**：不依赖特定网络结构（仅需满足覆盖数条件），并针对ReLU网络给出具体收敛速率。
- **实验亮点**：
  - 合成数据实验清晰展示了AD-Seq在**小样本下仍能恢复时间相关性**的优势。
  - 下游决策实验使用真实S&P 500数据，直接验证方法在金融组合优化中的实际价值，比单纯评估分布一致性更有说服力。
  - 对比设置严谨：区分了plug-in和RL两种策略，并对比了不同数据增强来源（合成vs bootstrap）。

## 8. 不足与局限
- **实验覆盖不足**：
  - 缺少高维或长序列（H>500）的测试；缺少与其他时间序列扩散模型（如Diffusion-TS, TSGM, TimeGAN）的直接对比。
  - 未进行消融实验：如历史截断长度、条件分数网络设计（是否用Transformer vs MLP）、训练数据量对结果的影响。
  - 未报告计算资源消耗，使复现和公平比较困难。
- **应用限制**：
  - 仅测试了金融场景，但方法对**马尔可夫性较强**的金融数据（如S&P 500）可能有利，对长记忆性数据（如某些经济指标）的理论优势尚未实证。
  - 需要假设时间依赖性衰减（Assumption 4.1），若数据具有长程依赖，截断机制可能引入较大误差。
- **理论局限**：
  - 理论分析假设条件分布Hölder光滑、次高斯性等，实际数据可能不严格满足。
  - 总变差界中因子 \( \sqrt{t_0} \) 和 \( \exp(-T) \) 在有限时间离散化中可能不够紧。
- **公平性风险**：合成数据在组合优化中提升夏普比率，但未评估其对风险把控（如尾部风险、最大回撤）的潜在偏差，可能诱导过度自信。

（完）
