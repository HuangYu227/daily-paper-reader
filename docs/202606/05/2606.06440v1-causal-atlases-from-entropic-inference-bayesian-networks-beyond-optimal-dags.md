---
title: "Causal Atlases from Entropic Inference: Bayesian Networks beyond Optimal DAGs"
title_zh: 从熵推断产生的因果图谱：超越最优有向无环图的贝叶斯网络
authors: "Hazhir Aliahmadi, Irina Babayan, Greg van Anders"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06440v1"
tags: ["query:tsc"]
score: 8.0
evidence: 基于熵推断的贝叶斯网络与因果图谱
tldr: 传统因果发现通过优化单一有向无环图(DAG)建模，但数据可能蕴含多种因果链，导致伪影。本文基于熵推断生成因果图集，采样最大熵图集以量化结构歧义。在2节点和20节点线性结构方程模型模拟中，优化DAG包含跨等价拓扑不一致的因果伪影。该方法提供更数据忠实的因果表示，超越单一最优DAG的局限性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06440v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1397, \"height\": 696, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.06440v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1415, \"height\": 465, \"label\": \"Figure\"}]"
motivation: 数据中固有多种因果链，优化单一DAG难以忠实反映，需更鲁棒的因果表示方法。
method: 基于熵推断采样最大熵图集，量化因果关系的结构歧义。
result: 在模拟线性结构方程模型中，优化DAG存在跨等价拓扑不一致的伪影。
conclusion: 熵推断生成的因果图集比单一优化DAG更忠实反映数据，提供多因果地图表示。
---

## 摘要
数据驱动的因果关系识别对于加深对科学内外复杂系统的理解至关重要。贝叶斯网络通过有向无环图（DAG）提供了一种用于建模通用因果关系的概率方法。然而，构建贝叶斯网络的典型技术依赖于优化，这可能不适合学习因果关系，因为底层数据可能允许多个因果链。更具数据忠实性的因果关系表示将为构建多个因果图谱提供框架，这些图谱与底层数据中固有的变异性一致。在这里，我们展示了基于熵的推断能够生成与底层数据一致的合理因果关系的图谱。在2节点和20节点线性结构方程模型的模拟噪声数据上，我们采样了一个最大熵图集，使我们能够量化底层因果关系中固有的结构模糊性。我们的方法表明，“优化”的DAG可能包含在同等准确的拓扑结构中不一致的因果伪影。

## Abstract
Data-driven causal relationship identification is pertinent to advancing understanding of complex systems both within and beyond science. Bayesian networks offer a probabilistic method for modelling generic causal relationships via directed acyclic graphs (DAGs). However, typical techniques for constructing Bayesian networks rely on optimization, which can be ill-suited for learning causal relationships because the underlying data may admit multiple chains of causation. More data-faithful representations of causal relationships would provide frameworks for constructing multiple causal maps that are consistent with the variability that is inherent in underlying data. Here, we show that entropy-based inference generates atlases of plausible causal relationships that are consistent with underlying data. On simulated noisy data of 2- and 20-node linear structural equation models, we sample a maximum-entropy ensemble of graphs that allow us to quantify the inherent structural ambiguity in underlying causal relationships. Our method shows that "optimized" DAGs can contain causal artifacts are not consistent across equivalently accurate topologies.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：传统因果发现方法（如基于评分的结构学习）通常通过优化损失函数输出一个“最优”有向无环图（DAG）。然而，实际数据可能支持多个因果链，优化单一DAG会人为地二值化因果关系，忽略固有的结构歧义，甚至产生不稳定的伪影（causal artifacts）。
- **背景与动机**：贝叶斯网络是因果建模的重要工具，但学习DAG面临组合爆炸和非凸约束等挑战。近年来连续优化方法（如NOTEARS、DAGMA）通过可微的环约束实现了可扩展性，但它们收敛到单一解，无法反映数据中可能存在的多候选因果结构。作者从统计物理视角提出：真实的因果结构不应由单个“最优”图表述，而应由一个合理的图集（atlas）来刻画，即一组与数据一致的DAG构成的系综。

#### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：采用最大熵推断（Maximum Entropy Inference）构建权重矩阵W上的吉布斯分布 p(W|X) ∝ exp(-β Q(W;X))，其中Q是评分函数（如数据拟合损失）。该分布不预设关于图结构的先验，而是由评分景观的几何形状诱导出隐式先验（entropic prior）。通过分子动力学（Simmering方法）对该分布进行采样，获得大量加权图样本（可能含环），再通过非线性投影（基于log-det环函数）将每个样本映射到DAG空间，最终得到DAG系综。
- **关键技术细节**：
  - **评分函数**：使用平方损失 Q_data(W) = ½n ||X - XW||²_F（线性SEM场景）。
  - **最大熵分布**：引入温度参数β，系综概率正比于exp(-β Q(W;X))。
  - **采样方法**：采用Nosé-Hoover链恒温器驱动的分子动力学（Simmering），将权重矩阵元素视为粒子位置，在扩展相空间中模拟轨迹，从而生成符合吉布斯分布的样本。
  - **环约束处理**：使用DAGMA提出的log-determinant环函数 h⁵_ldet(W) = -log det(sI - W∘W) + d log s，其梯度只作用于环边。对每个采样得到的W，沿梯度流的方向最小化h_ldet直到其接近0，从而投影到DAG空间。
  - **不确定性量化**：计算边缘概率p(edge|X)、边权重方差、以及系综内平均结构汉明距离（SHD）来衡量结构多样性。
- **算法流程**：
  1. 定义评分函数Q(W;X)和环函数h_ldet。
  2. 用Simmering方法以固定温度T=1/β采样W（平衡后收集轨迹）。
  3. 对每个采样W，通过梯度流投影到DAG（停止条件h_ldet ≤ δ）。
  4. 阈值化（忽略极小权重的边），得到DAG系综。
  5. 从系综中计算边缘概率、方差和平均SHD等统计量。

#### 3. 实验设计：使用的数据集/场景、benchmark、对比方法

- **实验场景**：
  - **2节点线性SEM（示意图明）**：真实图 X₁→X₂，生成1000个样本（高斯噪声，a≈0.9）。仅用于可视化观察采样和投影行为。
  - **20节点线性SEM（标准基准）**：采用Erdős–Rényi（ER4）随机图，期望度4，边权均匀分布在[-2,-0.5]∪[0.5,2]。生成1000个样本，噪声为N(0,1)。此设置与NOTEARS/DAGMA论文中的标准基准一致。
- **Benchmark/对比方法**：主要对比的是DAGMA（一种连续优化方法，使用相同的log-det环函数和评分函数，但只输出一个优化DAG）。论文并未与其他贝叶斯方法（如DiBS、BayesDAG、ProDAG）进行定量比较，仅于引言中提及它们存在先验假设。
- **评估指标**：对于2节点，展示权重分布和投影结果；对于20节点，报告系综的平均SHD、边缘概率图、以及选定边的权重分布与方差。

#### 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量或训练时长**。只给出了采样设置的迭代步数：2节点实验中，平衡1×10⁶步，采样5×10⁶步；20节点实验中，平衡1.2×10⁵步，收集4×10⁴个样本（从中随机选1×10⁴投影）。未提及总计算时间或硬件细节。

#### 5. 实验数量与充分性

- **实验数量**：仅两个简单模拟场景（2节点示意图 + 20节点线性SEM）。没有在更大规模（如d=50,100）或非线性模型中验证；没有与其他贝叶斯方法（DiBS等）进行定量比较；没有进行消融实验（如不同温度β对系综多样性的影响、不同投影容忍度δ的影响等）。
- **充分性与公平性**：
  - 实验设计较弱，不足以充分证明方法在实际问题中的优越性。20节点ER4是标准基准，但缺少与多种基线（包括NOTEARS, DAGMA, DiBS, BayesDAG）的量化对比（如SHD vs. ground truth、F1分数等）。
  - 文中仅用定性分析边缘概率和方差说明单一优化DAG可能包含伪影，但未提供系统性的性能表格。温度参数的选取没有详细讨论，也未展示不同温度下的行为变化。
  - 总体而言，实验偏初步验证，充分性不足。

#### 6. 论文的主要结论与发现

- **主要结论**：基于最大熵推断生成的DAG系综能够揭示单一优化DAG所隐藏的结构不确定性。优化得到的“最优”DAG可能包含因果伪影，而这些伪影在同等准确的拓扑中并不一致。边概率和权重方差的分布提供了比点估计更丰富的因果信息。
- **具体发现**：
  - 在2节点系统中，采样权重投影后形成两个模式（X₁→X₂和X₂→X₁），其中数据一致的方向概率更高，而优化方法只能返回一个解。
  - 在20节点系统中，系综内平均SHD=40（相对平均非零边数119），表明高拓扑多样性。一些边在单一优化图中存在，但在系综中呈现高方差，暗示方向歧义或伪影；另一些边具有高概率和低方差，表示强支持的因果关系。

#### 7. 优点

- **理论创新**：将统计物理的系综思想和最大熵原理引入因果结构学习，避免预设的结构先验，由数据自然诱导出隐式先验。
- **克服单一优化局限**：输出一组DAG而并非一个，提供边概率和方差，更好地反映了数据中的不确定性。
- **采样方法可扩展**：Simmering分子动力学结合连续优化框架，可扩展到较高维（20节点），且与可微环函数兼容。
- **环投影策略合理**：利用log-det环函数的梯度几何性质进行非线性投影，保留了系综的统计权重。

#### 8. 不足与局限

- **实验覆盖不足**：仅在线性SEM下测试，未在非线性模型（如非参数SEM）、真实数据集或更大规模图上验证。缺少与其他贝叶斯方法的定量比较。
- **温度β的选择**：文中未提供选择温度的策略，不同温度会影响系综多样性，缺乏指导原则。
- **投影步骤可能引入偏差**：在投影过程中（梯度流到h_ldet接近0）可能导致原本无环的样本发生变形，且对于不同样本投影的路径长度不同，可能影响系综的权重。论文未分析该偏差。
- **计算开销**：需要长时间采样和后续投影，相比单次优化成本更高。文中未比较效率。
- **阈值化问题**：投影后需要阈值去除极小权重，但阈值选择会直接影响边缘概率和方差，文中未讨论灵敏度。
- **潜在应用限制**：当数据信噪比极低或存在隐藏变量时，系综可能过度分散，难以提取可靠信息。该方法目前仅适用于观测数据，未考虑干预或部分混淆。

（完）
