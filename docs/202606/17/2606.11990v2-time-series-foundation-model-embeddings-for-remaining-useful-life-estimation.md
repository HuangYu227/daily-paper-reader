---
title: Time-Series Foundation Model Embeddings for Remaining Useful Life Estimation
title_zh: 用于剩余使用寿命估计的时间序列基础模型嵌入
authors: "Amir El-Ghoussani, Michele De Vita, Ronald Naumann, Vasileios Belagiannis"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11990v2"
tags: ["query:fe"]
score: 9.0
evidence: 冻结预训练时间序列基础模型用于特征提取
tldr: 工业预测维护中剩余寿命预测至关重要，但现有方法依赖大量标注数据和特征工程。本文提出轻量方法：冻结预训练时间序列基础模型Chronos-2作为特征提取器，搭配小型回归网络。实验表明，在两种工业传感器数据上，该方法优于RNN、CNN、Transformer及梯度提升基线，且更长的上下文历史显著提升性能。该工作为工业RUL估计提供了数据高效、无需繁琐特征工程的实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11990v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 929, \"height\": 758, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11990v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 909, \"height\": 420, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11990v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 835, \"height\": 359, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11990v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 587, \"height\": 195, \"label\": \"Table\"}]"
motivation: 现有RUL预测方法依赖大量标注数据和特征工程，数据效率低。
method: 使用冻结的Chronos-2基础模型提取上下文特征，训练轻量回归头进行RUL预测。
result: 在多传感器工业数据上，该方法在相同协议下优于多种序列模型和集成方法，且性能随上下文长度增加而提升。
conclusion: 预训练时间序列基础模型嵌入为工业RUL估计提供了数据高效、实用的替代方案。
---

## 摘要
剩余使用寿命（RUL）预测对于工业预测性维护至关重要，然而许多基于学习的方法依赖于大量的特征工程或大规模的标注数据集来训练特定任务的序列模型。在这项工作中，我们引入了一种轻量学习方法，利用冻结的预训练时间序列基础模型（TSFM），并将其与一个小型回归头相结合，用于从多变量传感器流中进行RUL估计。具体而言，我们使用Chronos-2作为冻结骨干网络来提取上下文窗口特征，并训练一个轻量级回归神经网络进行RUL预测。在来自两种设备类型的真实工业传感器数据上的实验表明，在相同的预处理和评估方案下，Chronos-2特征在循环、卷积、基于Transformer和梯度提升基线上持续改进。我们进一步分析了上下文长度的影响，发现更长的历史记录显著提升性能，这表明TSFM表示为工业环境中的RUL估计提供了一种实用且数据高效的替代方案。

## Abstract
Remaining Useful Life (RUL) prediction is essential for industrial predictive maintenance, yet many learning-based approaches rely on extensive feature engineering or large labeled datasets to train task-specific sequence models. In this work, we introduce a lightweight learning approach, in which we leverage a frozen pretrained time-series foundation model (TSFM) and combine it with a small regression head for RUL estimation from multivariate sensor streams. More specifically, we use Chronos-2 as a frozen backbone to extract context window features and train a lightweight regression neural network for RUL prediction. Experiments on real-world industrial sensor data from two device types show that Chronos-2 features consistently improve over recurrent, convolutional, Transformer-based, and gradient-boosting baselines under the same preprocessing and evaluation protocol. We further analyze the impact of context length and find that performance improves significantly with longer histories, indicating that TSFM representation offer a practical and data-efficient alternative for RUL estimation in industrial settings.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：在工业预测性维护中，剩余使用寿命（RUL）预测是提前规划干预、减少意外停机和成本的关键。传统数据驱动方法或依赖手工特征工程，或需要大量标注数据训练专用序列模型，存在数据效率低、泛化能力差、资源消耗大等问题。
- **背景**：时间序列基础模型（如Chronos-2）已在一系列预测任务中展现出强大的表征学习能力，但主要被用于时间序列预测（forecasting），尚未被系统性地应用到RUL估计这类回归任务中。
- **核心贡献**：本文提出一种轻量级方法，利用冻结的预训练TSFM（Chronos-2）作为特征提取器，仅训练一个小型回归头，实现高效、数据驱动的RUL估计，无需特征工程或大规模模型训练。

## 2. 方法论

### 核心思想
- 将Chronos-2视为一个固定的“上下文特征提取器”，从多变量传感器窗口数据中提取高维时序嵌入；然后通过一个轻量的多层感知器（MLP）回归头映射到标量RUL值。
- 保持TSFM参数冻结，仅优化回归头参数，极大降低了训练成本和标注数据需求。

### 关键技术细节
1. **数据预处理**：
   - 对不规则采样的传感器数据进行线性插值到均匀时间网格（步长 \(\Delta t = 1\) 小时）。
   - 丢弃长时间间隔缺口（超过阈值 \(\Delta t_{\text{max}}\)）。
   - 全局离群值裁剪到1%和99%分位数，并归一化为零均值单位方差（统计量仅从训练集计算）。
   - RUL标签：根据维修日志中下一次故障时间计算，并截断至最大1000天。

2. **模型架构**：
   - 骨干网络：Chronos-2（decoder-only transformer），参数冻结。
   - 上下文提取：取长度为 \(L\) 的窗口 \(X_{t-L:t}\)，输入Chronos-2得到嵌入序列 \(H \in \mathbb{R}^{L \times h}\)。
   - 回归头：2层MLP（隐含层宽度 \(m\)，ReLU激活，Dropout正则化），输出非负RUL预测值。

3. **训练**：
   - 损失函数：MSE（均方误差）。
   - 优化器：Adam，学习率 \(10^{-3}\)，批次大小64，最多50个epoch。
   - 仅优化MLP头参数，骨干网络不更新。

## 3. 实验设计

- **数据集**：
  - 来自Nokia Solutions and Networks GmbH & Co. KG的工业传感器数据，包含两种不同设备（Device A和Device B）。
  - Device A：297,345个标注样本，87个传感器通道。
  - Device B：119,364个标注样本，51个传感器通道。
  - 均为真实工业多变量传感器流。

- **基准（Benchmark）**：
  - 非序列基线：线性回归、随机森林。
  - 序列基线：GRU、LSTM、TCN（时序卷积网络）、Transformer编码器、梯度提升（基于统计特征）。
  - 所有基线使用与本文相同的预处理、窗口长度 \(L=5\)（主实验）和时间顺序分割（85:15训练/测试，避免时间泄漏）。

- **对比方法**：总共10种方法（包括本文方法）。

## 4. 资源与算力

- 文中明确提及：
  - 训练设备：单块NVIDIA A6000 GPU。
  - 训练时间：约2小时（每个设备）。
  - 模型参数量：Device A约300K，Device B约250K（仅回归头参数，骨干网络冻结）。
- 资源需求较低，体现了轻量级特点。

## 5. 实验数量与充分性

- **主要实验**：
  1. **主实验**（Table I）：在 \(L=5\) 窗口下，对比所有方法在Device A和Device B上的MAE和MSE。
  2. **上下文长度消融**（Figure 2）：在Device A上，变化 \(L \in \{5, 10, 20, 40, 80, 100\}\)，比较本文方法与多个基线。
  3. **回归头架构消融**（Table II）：在Device A上，比较线性头、2层MLP、4层MLP，并对比TCN基线。
- **充分性评价**：
  - 覆盖了两个独立设备数据集，验证了方法跨设备的泛化能力。
  - 消融实验系统分析了两个关键因素（上下文长度、回归头容量）。
  - 采用了严格的时间顺序分割，避免了数据泄漏，公平性较好。
  - 但缺少对更多公共数据集（如C-MAPSS）的验证，实验范围相对有限（仅工业私域数据）。

## 6. 主要结论与发现

- **结论1**：冻结的Chronos-2嵌入在RUL估计上显著优于所有基线（包括LSTM、GRU、TCN、Transformer、梯度提升等），在Device A上MAE降低超过50%（相比最强基线TCN）。
- **结论2**：上下文长度是关键因素：从 \(L=5\) 增加到 \(L=80\)，MAE降低约2倍；继续增加至100性能饱和。而基线方法对较长上下文没有明显增益。
- **结论3**：即使使用线性头，Chronos-2嵌入已取得优于TCN的结果（MAE 60 vs 88），表明骨干网络提供了强表征；2层MLP进一步改进，但更深层（4层）收益微小，说明信息主要来自预训练表示。

## 7. 优点

- **方法创新**：将时间序列基础模型从预测迁移到RUL估计，利用冻结表征实现数据高效学习，思路简洁实用。
- **轻量高效**：仅需训练小型MLP头（约250-300K参数），在单GPU上2小时完成，适合工业部署场景。
- **实证充分**：在两种不同工业设备数据上验证，并做了关键消融（上下文长度、头架构），结果一致性高。
- **实验设计严谨**：使用时间顺序分割，避免未来信息泄露；预处理统计量仅从训练集计算；基线超参数经独立验证集调优。

## 8. 不足与局限

- **数据集单一**：仅使用了Nokia的私有工业传感器数据，未在公共基准（如C-MAPSS涡轮发动机数据集、PHM挑战数据）上验证，泛化性证据不足。
- **依赖固定采样和插值**：线性插值假设传感器值在缺失区间内线性变化，可能不适用于快速退化或非平稳工况。
- **未处理多变量通道间的交互**：Chronos-2原本为单变量预测设计，本文直接输入多变量序列，未讨论如何有效融合不同通道的交互信息。
- **缺少不确定性量化**：虽然提到未来工作考虑概率输出，但当前方法仅提供点估计，对高风险决策场景不够可靠。
- **实验未覆盖低标注数据场景**：文中声称“数据高效”，但未在少样本或弱监督设置下验证方法的实际收益。
- **上下文长度上限未深入探索**：仅测试到L=100，且饱和点可能因设备类型而异，未分析最优点与退化过程的物理关系。

（完）
